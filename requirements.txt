# ZeroHour — Python dependencies
# Install with: pip install -r requirements.txt

# Core
streamlit>=1.35.0
pandas>=2.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Sentiment
vaderSentiment>=3.3.2

# Web scraping
beautifulsoup4>=4.12.0
googlesearch-python>=1.2.3
requests>=2.31.0

# Image processing & OCR
Pillow>=10.0.0
pytesseract>=0.3.10
python-multipart>=0.0.6
opencv-python>=4.8.0

# Optional: live Twitter scraping (may need older version)
# snscrape>=0.7.0

# LLM runtime (Ollama runs as a separate process — not a pip package)
# Install Ollama from https://ollama.com and run:
#   ollama pull phi3:mini
